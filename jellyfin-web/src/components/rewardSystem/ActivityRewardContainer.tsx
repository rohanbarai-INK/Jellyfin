import React from 'react';

import { REWARD_OVERLAY_ROOT_ID } from './RewardSystem';

const ActivityRewardContainer = () => (
    <div id={REWARD_OVERLAY_ROOT_ID} />
);

export default ActivityRewardContainer;
